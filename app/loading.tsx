import LoadingSpinner from "./components/LoadingSpinner";

/** Loading state for the home page, shown while data is being fetched. */
export default function Loading() {
  return <LoadingSpinner />;
}